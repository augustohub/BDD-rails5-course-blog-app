<h2><%= @article.title %></h2>
<small class="text-secondary">
  <b>Published by Author on
    <%= @article.created_at.strftime("%d/%m/%Y às %H:%M") %>
  </b>
  <%= link_to 'Edit', edit_article_path %> |
  <%= link_to 'Delete', @article,
                        method: :delete,
                        data: { confirm: 'Are you sure you want to delete this article?' } %>
</small>
<br>
<p><%= @article.body %></p>
